# Default values for deploy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
 # repository: devopsjfrog18.jfrog.io/ekart1-docker/ekart:1.0.0
  repository: vasusav/ekart
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: []
service:
  type: LoadBalancer
  port: 80

nodeSelector: {}

tolerations: []

affinity: {}
